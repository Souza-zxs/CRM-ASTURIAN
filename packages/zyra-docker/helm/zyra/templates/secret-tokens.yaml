{{- if .Values.secrets.tokens.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.tokens.name | default "tokens" }}
  namespace: {{ include "zyra.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "zyra.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: tokens
stringData:
  accessToken: {{ include "zyra.secret.tokens.access" . | quote }}
{{- end }}
