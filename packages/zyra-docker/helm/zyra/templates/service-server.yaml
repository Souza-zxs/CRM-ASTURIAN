{{- if .Values.server.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zyra.fullname" . }}-server
  namespace: {{ include "zyra.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "zyra.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: server
spec:
  type: {{ .Values.server.service.type | default "ClusterIP" }}
  selector:
    app.kubernetes.io/name: {{ include "zyra.name" . }}
    app.kubernetes.io/component: server
  ports:
    - name: http-tcp
      port: {{ .Values.server.service.port }}
      targetPort: {{ include "zyra.server.containerPort" . }}
      protocol: TCP
{{- end }}
