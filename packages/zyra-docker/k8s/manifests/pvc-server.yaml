apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zyracrm-server-pvc
  namespace: zyracrm
spec:
  storageClassName: default
  volumeName: zyracrm-server-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
