apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: zyracrm-server
  name: zyracrm-server
  namespace: zyracrm
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app: zyracrm-server
  template:
    metadata:
      labels:
        app: zyracrm-server
    spec:
      volumes:
        - name: zyracrm-server-data
          persistentVolumeClaim:
            claimName: zyracrm-server-pvc
        - name: zyracrm-docker-data
          persistentVolumeClaim:
            claimName: zyracrm-docker-data-pvc
      containers:
        - name: zyracrm
          image: zyracrm/zyra:latest
          imagePullPolicy: Always
          env:
            - name: NODE_PORT
              value: 3000
            - name: SERVER_URL
              value: "https://crm.example.com:443"
            - name: "PG_DATABASE_URL"
              value: "postgres://postgres:postgres@zyracrm-db.zyracrm.svc.cluster.local/default"
            - name: "REDIS_URL"
              value: "redis://zyracrm-redis.zyracrm.svc.cluster.local:6379"
            - name: SIGN_IN_PREFILLED
              value: "false"
            - name: STORAGE_TYPE
              value: "local"
            - name: "ACCESS_TOKEN_EXPIRES_IN"
              value: "7d"
            - name: "LOGIN_TOKEN_EXPIRES_IN"
              value: "1h"
            - name: APP_SECRET
              valueFrom:
                secretKeyRef:
                  name: tokens
                  key: accessToken
          ports:
            - containerPort: 3000
              name: http-tcp
              protocol: TCP
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1024Mi"
              cpu: "1000m"
          stdin: true
          tty: true
          volumeMounts:
            - mountPath: /app/docker-data
              name: zyracrm-docker-data
            - mountPath: /app/packages/zyra-server/.local-storage
              name: zyracrm-server-data
      dnsPolicy: ClusterFirst
      restartPolicy: Always
