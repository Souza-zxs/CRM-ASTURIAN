---
title: الخادم المحلي
description: إدارة خادم Zyra المحلي المستند إلى Docker — بدء التشغيل، الإيقاف، الترقية، مثيل اختبار متوازٍ، وإعداد SDK اليدوي.
icon: server
---

## إدارة الخادم المحلي

استخدم `yarn zyra docker:*` للتحكّم في حاوية Zyra المحلية:

| أمر                                    | ماذا يفعل                                          |
| -------------------------------------- | -------------------------------------------------- |
| `yarn zyra docker:start`             | بدء تشغيل الخادم (يسحب الصورة إذا لزم الأمر)       |
| `yarn zyra docker:start 2.2.0`       | بدء إصدار محدد من الخادم                           |
| `yarn zyra docker:start --port 3030` | بدء التشغيل على منفذ مخصّص                         |
| `yarn zyra docker:stop`              | إيقاف الخادم (مع الحفاظ على البيانات)              |
| `yarn zyra docker:status`            | عرض عنوان URL والإصدار وبيانات اعتماد تسجيل الدخول |
| `yarn zyra docker:logs`              | بث سجلات الخادم                                    |
| `yarn zyra docker:reset`             | مسح البيانات والبدء من جديد                        |
| `yarn zyra docker:upgrade`           | سحب أحدث صورة `zyra-app-dev`                     |
| `yarn zyra docker:upgrade 2.2.0`     | الترقية إلى إصدار محدد                             |

تظل البيانات محفوظة عبر عمليات إعادة التشغيل في وحدتي تخزين Docker (`zyra-app-dev-data` لـ PostgreSQL، و`zyra-app-dev-storage` للملفات). استخدم `reset` لمسح كل شيء.

## تثبيت إصدار الخادم

عند عدم تمرير أي إصدار، يقوم `docker:start` باشتقاق الإصدار من النطاق `engines.zyra` في ملف `package.json` لتطبيقك — وهو نفس النطاق الذي يتحقق منه الخادم عند تثبيت تطبيقك. يشغّل أحدث صورة منشورة لـ `zyra-app-dev` التي تُلبي هذا النطاق، مع الرجوع إلى `latest` عندما يكون الحقل غير موجودًا أو لا يتطابق أي إصدار منشور:

```json filename="package.json"
{
  "engines": {
    "zyra": ">=2.2.0"
  }
}
```

مرِّر إصدارًا بشكلٍ صريح لتجاوز النطاق لتشغيلٍ واحد فقط: `yarn zyra docker:start 2.3.0`. إذا كانت هناك حاوية موجودة بالفعل على إصدار مختلف، يقوم `docker:start` بترقيتها في مكانها (مع إعادة إنشاء الحاوية مع الحفاظ على وحدات تخزين بياناتك).

## ترقية صورة الخادم

يقوم `yarn zyra docker:upgrade` بسحب أحدث صورة، ومقارنة التجزئات، ولا يعيد إنشاء الحاوية إلا إذا حدث تغيير فعلي. تظل وحدات التخزين محفوظة — ويتم استبدال الحاوية فقط. إذا تم سحب صورة جديدة وكانت الحاوية تعمل، فستبدأ عملية الترقية تلقائيًا حاوية جديدة؛ شغّل بعد ذلك `yarn zyra docker:start` للانتظار حتى تصبح سليمة.

```bash filename="Terminal"
yarn zyra docker:upgrade            # Latest
yarn zyra docker:upgrade 2.2.0      # Specific version
```

تحقّق من الإصدار الجاري باستخدام `yarn zyra docker:status` (يعرض قيمة `APP_VERSION` المضمنة في الحاوية).

## تشغيل مثيل اختبار متوازٍ

مرّر `--test` إلى أي أمر `docker:*` لإدارة مثيل ثانٍ معزول تمامًا — مفيد لاختبارات التكامل أو للتجربة من دون لمس بيانات التطوير الرئيسية لديك:

| أمر                                 | ماذا يفعل                                 |
| ----------------------------------- | ----------------------------------------- |
| `yarn zyra docker:start --test`   | بدء مثيل الاختبار (المنفذ الافتراضي 2021) |
| `yarn zyra docker:stop --test`    | إيقافه                                    |
| `yarn zyra docker:status --test`  | عرض حالته                                 |
| `yarn zyra docker:logs --test`    | بث سجلاته                                 |
| `yarn zyra docker:reset --test`   | مسح بياناته                               |
| `yarn zyra docker:upgrade --test` | ترقية صورته                               |

يملك مثيل الاختبار حاويته الخاصة (`zyra-app-dev-test`) ووحدات التخزين الخاصة به (`zyra-app-dev-test-data`، `zyra-app-dev-test-storage`) وتهيئته — ويعمل جنبًا إلى جنب مع مثيلك الرئيسي بدون تعارضات. اجمع `--test` مع `--port` لتجاوز المنفذ 2021.

## إعداد يدوي (بدون أداة توليد الهيكل)

تجاوز أداة توليد الهيكل إذا كنت تضيف SDK إلى مشروع قائم:

```bash filename="Terminal"
yarn add zyra-sdk zyra-client-sdk
```

أضِف النص البرمجي إلى `package.json`:

```json filename="package.json"
{
  "scripts": {
    "zyra": "zyra"
  }
}
```

يمكنك الآن تشغيل `yarn zyra dev`، و`yarn zyra docker:start`، والبقية.

<Note>
لا تثبّت `zyra-sdk` عالميًا — ثبِّته لكل مشروع بحيث يستخدم كل تطبيق إصداره الخاص.
</Note>
