---
title: Řešení potíží
description: Časté problémy při prvním spuštění — Docker, verze Node, Yarn, závislosti.
icon: klíč
---

* **Chyby Dockeru** — Před spuštěním `yarn zyra docker:start` se ujistěte, že Docker Desktop (nebo démon) běží. Chybová zpráva ukáže správný příkaz pro spuštění pro váš operační systém.
* **Nesprávná verze Node** — Je potřeba 24+. Ověřte pomocí `node -v`.
* **Chybí Yarn 4** — Spusťte `corepack enable`.
* **Rozbité závislosti** — `rm -rf node_modules && yarn install`.
* **Chyby `zyra-sdk` po upgradu na v2.8.0** — V 2.8.0 byl přesunut z `dependencies` do `devDependencies`. Viz [Struktura projektu → Závislosti](/l/cs/developers/extend/apps/getting-started/project-structure#dependencies).
* **`zyra build` upozorňuje na `zyra-client-sdk` v sekci `dependencies`** — je poskytován za běhu Zyra, takže by měl být přesunut do `devDependencies` vedle `zyra-sdk`. Viz [Struktura projektu → Závislosti](/l/cs/developers/extend/apps/getting-started/project-structure#dependencies).

Zasekli jste se? Zeptejte se na [Discordu Zyra](https://discord.com/channels/1130383047699738754/1130386664812982322).
