---
title: Lokální server
description: Spravujte lokální Docker server Zyra – spouštění, zastavení, upgrade, paralelní testovací instance a ruční nastavení SDK.
icon: server
---

## Správa lokálního serveru

K ovládání lokálního kontejneru Zyra použijte `yarn zyra docker:*`:

| Příkaz                                 | K čemu slouží                                  |
| -------------------------------------- | ---------------------------------------------- |
| `yarn zyra docker:start`             | Spustí server (v případě potřeby stáhne image) |
| `yarn zyra docker:start 2.2.0`       | Spustit konkrétní verzi serveru                |
| `yarn zyra docker:start --port 3030` | Spustí na vlastním portu                       |
| `yarn zyra docker:stop`              | Zastaví server (zachová data)                  |
| `yarn zyra docker:status`            | Zobrazí URL, verzi a přihlašovací údaje        |
| `yarn zyra docker:logs`              | Streamuje protokoly serveru                    |
| `yarn zyra docker:reset`             | Vymaže data a začne znovu                      |
| `yarn zyra docker:upgrade`           | Stáhne nejnovější image `zyra-app-dev`       |
| `yarn zyra docker:upgrade 2.2.0`     | Aktualizuje na konkrétní verzi                 |

Data přetrvávají při restartech ve dvou svazcích Dockeru (`zyra-app-dev-data` pro PostgreSQL, `zyra-app-dev-storage` pro soubory). Pomocí `reset` vymažte vše.

## Fixace verze serveru

Když není předána žádná verze, `docker:start` určí verzi z rozsahu `engines.zyra` vaší aplikace v `package.json` — stejného rozsahu, vůči kterému server ověřuje, když je vaše aplikace nainstalována. Spustí nejnovější publikovaný image `zyra-app-dev`, který splňuje tento rozsah, a pokud pole chybí nebo žádná publikovaná verze neodpovídá, použije `latest`:

```json filename="package.json"
{
  "engines": {
    "zyra": ">=2.2.0"
  }
}
```

Předáním verze můžete rozsah pro jedno spuštění přepsat: `yarn zyra docker:start 2.3.0`. Pokud už kontejner existuje v jiné verzi, `docker:start` jej na místě aktualizuje (znovu vytvoří kontejner při zachování vašich datových svazků).

## Aktualizace obrazu serveru

`yarn zyra docker:upgrade` stáhne nejnovější image, porovná digesty a znovu vytvoří kontejner pouze v případě, že se skutečně něco změnilo. Svazky zůstanou zachovány — nahradí se pouze kontejner. Pokud byl stažen nový image a kontejner běžel, upgrade automaticky spustí nový kontejner; poté spusťte `yarn zyra docker:start`, abyste počkali, než bude ve stavu 'healthy'.

```bash filename="Terminal"
yarn zyra docker:upgrade            # Latest
yarn zyra docker:upgrade 2.2.0      # Specific version
```

Běžící verzi ověříte pomocí `yarn zyra docker:status` (zobrazí `APP_VERSION` zabudovanou v kontejneru).

## Spuštění paralelní testovací instance

Předejte `--test` libovolnému příkazu `docker:*` pro správu druhé, plně izolované instance — užitečné pro integrační testy nebo experimentování bez zásahu do vašich hlavních vývojových dat:

| Příkaz                              | K čemu slouží                                    |
| ----------------------------------- | ------------------------------------------------ |
| `yarn zyra docker:start --test`   | Spustí testovací instanci (výchozí port je 2021) |
| `yarn zyra docker:stop --test`    | Zastaví ji                                       |
| `yarn zyra docker:status --test`  | Zobrazí její stav                                |
| `yarn zyra docker:logs --test`    | Streamuje její protokoly                         |
| `yarn zyra docker:reset --test`   | Vymaže její data                                 |
| `yarn zyra docker:upgrade --test` | Aktualizuje její image                           |

Testovací instance má vlastní kontejner (`zyra-app-dev-test`), svazky (`zyra-app-dev-test-data`, `zyra-app-dev-test-storage`) a konfiguraci — běží souběžně s vaší hlavní instancí bez konfliktů. Zkombinujte `--test` s `--port` pro změnu výchozího portu 2021.

## Ruční nastavení (bez generátoru kostry)

Pokud přidáváte SDK do existujícího projektu, generátor kostry přeskočte:

```bash filename="Terminal"
yarn add zyra-sdk zyra-client-sdk
```

Přidejte skript do `package.json`:

```json filename="package.json"
{
  "scripts": {
    "zyra": "zyra"
  }
}
```

Nyní můžete spouštět `yarn zyra dev`, `yarn zyra docker:start` a další.

<Note>
Neinstalujte `zyra-sdk` globálně — nainstalujte jej v každém projektu zvlášť, aby každá aplikace používala svou vlastní verzi.
</Note>
