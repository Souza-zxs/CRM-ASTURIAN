---
title: Yerel Sunucu
description: Yerel Zyra Docker sunucusunu yönetin — başlatın, durdurun, yükseltin, paralel test örneği çalıştırın ve manuel SDK kurulumu yapın.
icon: server
---

## Yerel sunucuyu yönetme

Yerel Zyra konteynerini kontrol etmek için `yarn zyra docker:*` kullanın:

| Komut                                  | Ne yapar                                                  |
| -------------------------------------- | --------------------------------------------------------- |
| `yarn zyra docker:start`             | Sunucuyu başlatır (gerekirse imajı çeker)                 |
| `yarn zyra docker:start 2.2.0`       | Belirli bir sunucu sürümünü başlatın                      |
| `yarn zyra docker:start --port 3030` | Özel bir portta başlatır                                  |
| `yarn zyra docker:stop`              | Sunucuyu durdurur (verileri korur)                        |
| `yarn zyra docker:status`            | URL'yi, sürümü ve oturum açma kimlik bilgilerini gösterir |
| `yarn zyra docker:logs`              | Sunucu günlüklerini akış olarak gösterir                  |
| `yarn zyra docker:reset`             | Verileri siler ve sıfırdan başlatır                       |
| `yarn zyra docker:upgrade`           | En güncel `zyra-app-dev` imajını çeker                  |
| `yarn zyra docker:upgrade 2.2.0`     | Belirli bir sürüme yükseltir                              |

Veriler, yeniden başlatmalar arasında iki Docker biriminde kalıcıdır (PostgreSQL için `zyra-app-dev-data`, dosyalar için `zyra-app-dev-storage`). Her şeyi silmek için `reset` kullanın.

## Sunucu sürümünü sabitleme

Herhangi bir sürüm iletilmediğinde, `docker:start` sürümü uygulamanızın `package.json` içindeki `engines.zyra` aralığından çözümler — bu, uygulamanız yüklendiğinde sunucunun doğruladığı aralıkla aynıdır. Bu komut, aralığı karşılayan en yeni yayımlanmış `zyra-app-dev` imajını başlatır; alan yoksa veya hiçbir yayımlanmış sürüm eşleşmiyorsa `latest` değerine geri döner:

```json filename="package.json"
{
  "engines": {
    "zyra": ">=2.2.0"
  }
}
```

Aralığı tek bir çalıştırma için geçersiz kılmak amacıyla sürümü açıkça iletin: `yarn zyra docker:start 2.3.0`. Bir kapsayıcı zaten farklı bir sürümde mevcutsa, `docker:start` onu yerinde yükseltir (veri birimlerinizi korurken kapsayıcıyı yeniden oluşturarak).

## Sunucu imajını yükseltme

`yarn zyra docker:upgrade`, en güncel imajı çeker, özetleri karşılaştırır ve yalnızca gerçekten bir şey değiştiyse konteyneri yeniden oluşturur. Birimler korunur — yalnızca konteyner değiştirilir. Yeni bir imaj çekildiyse ve konteyner çalışıyorsa, yükseltme otomatik olarak yeni bir konteyner başlatır; sağlıklı hale gelmesini beklemek için ardından `yarn zyra docker:start` çalıştırın.

```bash filename="Terminal"
yarn zyra docker:upgrade            # Latest
yarn zyra docker:upgrade 2.2.0      # Specific version
```

Çalışan sürümü `yarn zyra docker:status` ile doğrulayabilirsiniz (konteynere gömülü `APP_VERSION` değerini gösterir).

## Paralel bir test örneği çalıştırma

Ana geliştirme verilerinize dokunmadan entegrasyon testlerini veya denemeleri yürütmek için, herhangi bir `docker:*` komutuna `--test` parametresini vererek ikinci, tamamen yalıtılmış bir örneği yönetebilirsiniz:

| Komut                               | Ne yapar                                                      |
| ----------------------------------- | ------------------------------------------------------------- |
| `yarn zyra docker:start --test`   | Test örneğini başlatır (varsayılan bağlantı noktası 2021'dir) |
| `yarn zyra docker:stop --test`    | Durdurur                                                      |
| `yarn zyra docker:status --test`  | Durumunu gösterir                                             |
| `yarn zyra docker:logs --test`    | Günlüklerini akış olarak gösterir                             |
| `yarn zyra docker:reset --test`   | Verilerini siler                                              |
| `yarn zyra docker:upgrade --test` | İmajını yükseltir                                             |

Test örneği, kendine ait bir Docker konteynerinde (`zyra-app-dev-test`), ayrılmış birimlerle (`zyra-app-dev-test-data`, `zyra-app-dev-test-storage`) ve yapılandırmayla çalışır; böylece ana örneğinizle çakışma olmadan paralel olarak çalışabilir. Varsayılan 2021'i geçersiz kılmak için `--test` ile `--port`'u birlikte kullanın.

## Manuel kurulum (iskelet oluşturucu olmadan)

SDK'yı mevcut bir projeye ekliyorsanız iskelet oluşturma adımını atlayın:

```bash filename="Terminal"
yarn add zyra-sdk zyra-client-sdk
```

`package.json` dosyasına betiği ekleyin:

```json filename="package.json"
{
  "scripts": {
    "zyra": "zyra"
  }
}
```

Artık `yarn zyra dev`, `yarn zyra docker:start` ve diğerlerini çalıştırabilirsiniz.

<Note>
`zyra-sdk`'yi global olarak kurmayın — her uygulama kendi sürümünü kullansın diye proje bazında sabitleyin.
</Note>
