---
title: Server locale
description: Gestisci il server Docker Zyra locale — avvia, arresta, aggiorna, istanza di test parallela e configurazione manuale dell'SDK.
icon: server
---

## Gestire il server locale

Usa `yarn zyra docker:*` per controllare il container Zyra locale:

| Comando                                | Cosa fa                                            |
| -------------------------------------- | -------------------------------------------------- |
| `yarn zyra docker:start`             | Avvia il server (scarica l'immagine se necessario) |
| `yarn zyra docker:start 2.2.0`       | Avvia una versione specifica del server            |
| `yarn zyra docker:start --port 3030` | Avvia su una porta personalizzata                  |
| `yarn zyra docker:stop`              | Arresta il server (conserva i dati)                |
| `yarn zyra docker:status`            | Mostra URL, versione e credenziali di accesso      |
| `yarn zyra docker:logs`              | Trasmetti in streaming i log del server            |
| `yarn zyra docker:reset`             | Cancella i dati e riparti da zero                  |
| `yarn zyra docker:upgrade`           | Scarica l'ultima immagine `zyra-app-dev`         |
| `yarn zyra docker:upgrade 2.2.0`     | Aggiorna a una versione specifica                  |

I dati persistono tra i riavvii in due volumi Docker (`zyra-app-dev-data` per PostgreSQL, `zyra-app-dev-storage` per i file). Usa `reset` per cancellare tutto.

## Blocco della versione del server

Quando non viene passata alcuna versione, `docker:start` risolve la versione dall'intervallo `engines.zyra` della tua app in `package.json`, lo stesso intervallo rispetto al quale il server effettua la convalida quando la tua app viene installata. Avvia l'immagine `zyra-app-dev` più recente pubblicata che soddisfa l'intervallo, ricorrendo a `latest` quando il campo è assente o nessuna versione pubblicata corrisponde:

```json filename="package.json"
{
  "engines": {
    "zyra": ">=2.2.0"
  }
}
```

Passa esplicitamente una versione per ignorare l'intervallo per una singola esecuzione: `yarn zyra docker:start 2.3.0`. Se un container esiste già con una versione diversa, `docker:start` lo aggiorna sul posto (ricreando il container ma preservando i volumi dei tuoi dati).

## Aggiornamento dell'immagine del server

`yarn zyra docker:upgrade` scarica l'ultima immagine, confronta i digest e ricrea il container solo se è cambiato qualcosa. I volumi vengono preservati — viene sostituito solo il container. Se è stata scaricata una nuova immagine e il container era in esecuzione, l'upgrade avvia automaticamente un nuovo container; esegui poi `yarn zyra docker:start` per attendere che diventi operativo.

```bash filename="Terminal"
yarn zyra docker:upgrade            # Latest
yarn zyra docker:upgrade 2.2.0      # Specific version
```

Verifica la versione in esecuzione con `yarn zyra docker:status` (mostra `APP_VERSION` incorporata nel container).

## Esecuzione di un'istanza di test parallela

Passa `--test` a qualsiasi comando `docker:*` per gestire una seconda istanza completamente isolata — utile per test di integrazione o per sperimentare senza toccare i tuoi dati di sviluppo principali:

| Comando                             | Cosa fa                                                                  |
| ----------------------------------- | ------------------------------------------------------------------------ |
| `yarn zyra docker:start --test`   | Avvia l'istanza di test (per impostazione predefinita usa la porta 2021) |
| `yarn zyra docker:stop --test`    | Arrestala                                                                |
| `yarn zyra docker:status --test`  | Mostra il suo stato                                                      |
| `yarn zyra docker:logs --test`    | Trasmetti in streaming i suoi log                                        |
| `yarn zyra docker:reset --test`   | Cancella i suoi dati                                                     |
| `yarn zyra docker:upgrade --test` | Aggiorna la sua immagine                                                 |

L'istanza di test ha il proprio container (`zyra-app-dev-test`), i propri volumi (`zyra-app-dev-test-data`, `zyra-app-dev-test-storage`) e la propria configurazione — viene eseguita insieme alla tua istanza principale senza conflitti. Combina `--test` con `--port` per sovrascrivere 2021.

## Configurazione manuale (senza lo scaffolder)

Salta lo strumento di scaffolding se stai aggiungendo l'SDK a un progetto esistente:

```bash filename="Terminal"
yarn add zyra-sdk zyra-client-sdk
```

Aggiungi lo script a `package.json`:

```json filename="package.json"
{
  "scripts": {
    "zyra": "zyra"
  }
}
```

Ora puoi eseguire `yarn zyra dev`, `yarn zyra docker:start` e il resto.

<Note>
Non installare `zyra-sdk` globalmente — bloccalo per progetto in modo che ogni app usi la propria versione.
</Note>
