// The newest release, surfaced in the menu's Releases preview. Zyra doesn't
// have a published release history yet (RELEASE_NOTES is empty — see
// ReleaseNotes.tsx), so this points at a generic placeholder rather than a
// specific version until there's a real release to feature.
export const LATEST_RELEASE: {
  release: string;
  title: string;
  previewImage: string;
} = {
  release: 'Coming soon',
  title: 'Releases are on the way',
  previewImage: '/images/releases/milestone.webp',
};
