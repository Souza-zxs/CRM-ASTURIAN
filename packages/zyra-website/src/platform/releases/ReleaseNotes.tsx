import { type ReleaseNote } from './release-note';

// Zyra does not have its own release history to publish yet — the previous
// content here was Twenty's open-source changelog with the name swapped in,
// which doesn't reflect this product. Empty until Zyra ships its own
// releases; ReleasesFeed renders a placeholder when this is empty.
export const RELEASE_NOTES: ReleaseNote[] = [];
